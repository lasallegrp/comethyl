on:
  push:
    branches: [master]
  workflow_dispatch:

name: pkgdown

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Set up Pandoc
        uses: r-lib/actions/setup-pandoc@v2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxml2-dev libcurl4-openssl-dev libssl-dev \
            libharfbuzz-dev libfribidi-dev libgit2-dev \
            libfontconfig1-dev libfreetype6-dev \
            libglpk-dev libbz2-dev liblzma-dev \
            libpng-dev libjpeg-dev libtiff-dev

      - name: Install R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown
          needs: website

      - name: Install Bioconductor and vignette dependencies
        shell: Rscript {0}
        run: |
          install.packages("prettydoc")
          if (!requireNamespace("BiocManager", quietly = TRUE))
            install.packages("BiocManager")
          BiocManager::install(c(
            "bsseq", "GenomicRanges", "IRanges", "annotatr", "WGCNA",
            "sva", "clusterProfiler", "DOSE", "enrichplot", "fgsea",
            "ReactomePA", "rGREAT", "DelayedMatrixStats", "GenomeInfoDb",
            "SummarizedExperiment", "S4Vectors", "BiocGenerics",
            "preprocessCore", "impute", "dmrseq", "biomaRt",
            "org.Hs.eg.db"
          ), ask = FALSE, update = FALSE)

      - name: Install comethyl
        shell: Rscript {0}
        run: |
          install.packages(".", repos = NULL, type = "source")

      - name: Build and deploy pkgdown site
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          Rscript -e 'pkgdown::deploy_to_branch(new_process = FALSE)'